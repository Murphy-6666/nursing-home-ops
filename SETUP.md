# 快速部署指南

本指南帮助你从零搭建养老院运营管理系统，预计耗时 2-3 小时。

---

## 前置条件

- 飞书企业版账号（需多维表格、日历、IM、审批流功能）
- TRAE AI 助手（或兼容 SKILL 格式的 AI 工具）
- 基本的飞书操作能力

---

## 第一步：下载项目

```bash
git clone https://github.com/Murphy-6666/nursing-home-ops.git
cd nursing-home-ops
```

或直接在 GitHub 页面点击 "Code" → "Download ZIP" 下载解压。

---

## 第二步：创建飞书多维表格

### 方式一：CSV 导入（推荐，约 30 分钟）

1. 在飞书中新建一个多维表格（Base）
2. 点击右上角 "..." → "导入" → "选择 CSV 文件"
3. 按顺序导入以下 9 个 CSV 文件：

| 序号 | 文件 | 对应表名 | 说明 |
|------|------|---------|------|
| 01 | `templates/csv/01-residents.csv` | 老人基本信息表 | 核心基础表 |
| 02 | `templates/csv/02-care-records.csv` | 护理记录表 | 日常护理记录 |
| 03 | `templates/csv/03-medication.csv` | 用药方案表 | 用药管理 |
| 04 | `templates/csv/04-scheduling.csv` | 排班表 | 排班管理 |
| 05 | `templates/csv/05-health-data.csv` | 健康数据表 | 体征监测 |
| 06 | `templates/csv/06-finance.csv` | 费用台账表 | 财务管理 |
| 07 | `templates/csv/07-activities.csv` | 活动记录表 | 活动管理 |
| 08 | `templates/csv/08-employees.csv` | 员工表 | 辅助表 |
| 09 | `templates/csv/09-family-contacts.csv` | 家属联系表 | 家属沟通 |

每个 CSV 导入后会成为多维表格中的一个数据表（Sheet），CSV 中的第一行会自动成为字段名。

### 方式二：API 自动建表（适合开发者，约 10 分钟）

详见 `scripts/README.md`，通过飞书 OpenAPI 一键创建所有表格并设置字段类型。

---

## 第三步：设置字段类型

CSV 导入后所有字段默认为"文本"类型，需要手动调整为正确类型：

### 老人基本信息表

| 字段名 | 设置为 | 选项值 |
|--------|--------|--------|
| 性别 | 单选 | 男、女 |
| 年龄 | 公式 | `DATEDIF(出生年月, TODAY(), "Y")` |
| 护理等级 | 单选 | 自理、半护理、全护理、特护 |
| 饮食类型 | 单选 | 普食、软食、半流食、流食 |
| 饮食禁忌 | 多选 | 糖尿病饮食、低盐、低脂、低嘌呤、无麸质 |
| 过敏食物 | 多选 | 海鲜、鸡蛋、牛奶、坚果、其他 |
| 状态 | 单选 | 在住、出院、请假 |

### 护理记录表

| 字段名 | 设置为 | 选项值 |
|--------|--------|--------|
| 老人姓名 | 关联 | 关联 → 老人基本信息表.姓名 |
| 班次 | 单选 | 白班、中班、夜班 |
| 精神状态 | 单选 | 良好、一般、较差、嗜睡、烦躁 |
| 记录人 | 关联 | 关联 → 员工表.姓名 |
| 异常标记 | 公式 | 见下方公式 |

异常标记公式：
```
IF(OR(体温<36, 体温>37.3, 收缩压>140, 收缩压<90, 舒张压>90, 舒张压<60, 血糖>7.8, 血糖<3.9), "异常", "正常")
```

### 用药方案表

| 字段名 | 设置为 | 选项值 |
|--------|--------|--------|
| 老人姓名 | 关联 | 关联 → 老人基本信息表.姓名 |
| 服药频率 | 单选 | 每日1次、每日2次、每日3次、按需、其他 |
| 服药方式 | 单选 | 餐前、餐后、睡前、空腹、随餐 |
| 状态 | 单选 | 服用中、已停药、已完成 |

### 排班表

| 字段名 | 设置为 | 选项值 |
|--------|--------|--------|
| 护理员 | 关联 | 关联 → 员工表.姓名 |
| 班次 | 单选 | 白班(08-16)、中班(16-24)、夜班(00-08) |
| 负责区域 | 单选 | A区、B区、C区、全院 |
| 状态 | 单选 | 正常、请假、调班、空缺 |
| 替班人 | 关联 | 关联 → 员工表.姓名 |

### 健康数据表

| 字段名 | 设置为 | 选项值 |
|--------|--------|--------|
| 老人姓名 | 关联 | 关联 → 老人基本信息表.姓名 |
| 血糖类型 | 单选 | 空腹、餐后 |
| 异常等级 | 公式 | 见下方公式 |
| 记录人 | 关联 | 关联 → 员工表.姓名 |

异常等级公式：
```
IF(OR(体温>38, 体温<35.5, 收缩压>160, 收缩压<80, 舒张压>100, 舒张压<50, 心率>120, 心率<40, 血糖>11, 血糖<2.8), "危险",
  IF(OR(体温>37.3, 体温<36, 收缩压>140, 收缩压<90, 舒张压>90, 舒张压<60, 心率>100, 心率<60, 血糖>7.8, 血糖<3.9), "偏高偏低", "正常"))
```

### 费用台账表

| 字段名 | 设置为 | 选项值 |
|--------|--------|--------|
| 老人姓名 | 关联 | 关联 → 老人基本信息表.姓名 |
| 费用类别 | 单选 | 床位费、护理费、餐费、医疗费、额外服务 |
| 计费周期 | 单选 | 月结、日结、次结 |
| 状态 | 单选 | 未结算、已结算、已开票 |

### 活动记录表

| 字段名 | 设置为 | 选项值 |
|--------|--------|--------|
| 活动名称 | 关联 | 关联 → 活动方案表 |
| 活动类型 | 单选 | 运动、音乐、手工、认知、社交、节日 |
| 参与老人 | 多选关联 | 关联 → 老人基本信息表.姓名 |
| 参与度 | 单选 | 高、中、低 |
| 老人反馈 | 单选 | 积极、中性、消极 |
| 带领人 | 关联 | 关联 → 员工表.姓名 |

---

## 第四步：清理示例数据

导入的 CSV 中包含 2-3 行示例数据，用于展示数据格式。设置好字段类型后：

1. 选中示例数据行
2. 删除这些行
3. 录入你自己的真实数据

建议先从「员工表」和「老人基本信息表」开始录入。

---

## 第五步：配置自动化

### 5.1 异常预警（护理记录表）

1. 打开护理记录表 → "自动化" → "新建自动化"
2. 触发条件：当「异常标记」等于「异常」时
3. 执行动作：发送飞书消息给当班主管
4. 消息内容：`{老人姓名} 体征异常：体温{体温}，血压{收缩压}/{舒张压}，请及时查看`

### 5.2 用药提醒

1. 打开用药方案表 → "自动化" → "新建自动化"
2. 设置定时触发（每天 07:00）
3. 执行动作：生成当日用药清单，发送给当班护理员

### 5.3 家属每日推送

1. 打开护理记录表 → "自动化" → "新建自动化"
2. 设置定时触发（每天 20:00）
3. 执行动作：汇总当日每位老人的护理数据，按家属联系表发送摘要

### 5.4 月度账单

1. 打开费用台账表 → "自动化" → "新建自动化"
2. 设置定时触发（每月 1 日 08:00）
3. 执行动作：按老人汇总上月费用，生成账单推送给家属

---

## 第六步：配置仪表盘

在每个数据表中点击 "仪表盘" 添加图表：

### 护理记录表仪表盘
- 今日异常记录统计（柱状图）
- 本周各班次记录数（折线图）

### 健康数据表仪表盘
- 血压趋势（折线图，按老人筛选）
- 血糖趋势（折线图）
- 异常事件统计（饼图）

### 排班表仪表盘
- 今日在岗人员（按班次分组表格）
- 本周排班甘特图
- 月度夜班统计（柱状图）

### 费用台账表仪表盘
- 月度收入构成（饼图）
- 收入趋势（折线图）
- 欠费统计（表格）

### 活动记录表仪表盘
- 活动类型分布（饼图）
- 参与率趋势（折线图）

---

## 第七步：安装 SKILL

将 `SKILL.md` 文件安装到你的 TRAE AI 助手中：

1. 复制 `SKILL.md` 文件内容
2. 在 TRAE 中创建新 Skill
3. 粘贴内容并保存

之后你可以直接对 AI 说：
- "记录张奶奶今天血压 135/85，吃了降压药"
- "生成本周排班表"
- "发送张奶奶的家属周报"

---

## 常见问题

### Q: 没有飞书企业版怎么办？
A: 可以用免费版飞书的多维表格功能，但自动化触发条件和消息推送数量有限。建议至少使用飞书标准版。

### Q: CSV 导入后字段类型全是文本？
A: 这是正常的，CSV 格式不携带字段类型信息。请按本指南第三步手动设置。

### Q: 公式字段怎么设置？
A: 在多维表格中新增字段 → 选择"公式"类型 → 粘贴本指南中提供的公式。飞书公式语法与 Excel 类似。

### Q: 关联字段怎么设置？
A: 新增字段 → 选择"关联"类型 → 选择要关联的表 → 选择关联字段。关联后两个表的数据就可以互相引用。

### Q: 可以不用 TRAE AI 吗？
A: 可以。工作流文档本身是通用方案，任何支持自然语言交互的 AI 工具都可以配合使用，只要能读取多维表格数据即可。

---

## 部署检查清单

- [ ] 9 张多维表格已创建
- [ ] 所有字段类型已设置
- [ ] 关联字段已配置
- [ ] 公式字段已添加
- [ ] 示例数据已清理
- [ ] 真实数据已录入（员工、老人）
- [ ] 异常预警自动化已配置
- [ ] 用药提醒自动化已配置
- [ ] 家属推送自动化已配置
- [ ] 仪表盘已配置
- [ ] SKILL 已安装到 AI 助手
- [ ] 测试一条护理记录录入
- [ ] 测试一次家属推送

完成以上所有步骤后，系统即可投入日常使用。
