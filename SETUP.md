# 部署指南

两条路，选一条走完就行。

---

## 路线 A：CSV 导入（不会编程选这个）

### 第1步：下载项目

```bash
git clone https://github.com/Murphy-6666/nursing-home-ops.git
```

或到 GitHub 页面点 `Code` → `Download ZIP` 解压。

### 第2步：导入 CSV

1. 飞书中新建一个多维表格
2. 点右上角 `...` → `导入` → 选 CSV 文件
3. 按文件名序号依次导入 15 个 CSV（在 `templates/csv/` 目录下）

导入后每个 CSV 变成一个数据表，第一行自动成为字段名。

### 第3步：设置字段类型

CSV 导入后所有字段都是"文本"类型，需手动改。**按下面表格逐个设置：**

#### 老人基本信息表

| 字段 | 改为 | 选项/公式 |
|------|------|-----------|
| 性别 | 单选 | 男、女 |
| 年龄 | 公式 | `DATEDIF(出生年月, TODAY(), "Y")` |
| 护理等级 | 单选 | 自理、半护理、全护理、特护 |
| 饮食类型 | 单选 | 普食、软食、半流食、流食 |
| 饮食禁忌 | 多选 | 糖尿病饮食、低盐、低脂、低嘌呤、无麸质 |
| 过敏食物 | 多选 | 海鲜、鸡蛋、牛奶、坚果、其他 |
| 状态 | 单选 | 在住、出院、请假 |

#### 护理记录表

| 字段 | 改为 | 选项/公式 |
|------|------|-----------|
| 老人姓名 | 关联 | → 老人基本信息表.姓名 |
| 班次 | 单选 | 白班、中班、夜班 |
| 体温/收缩压/舒张压/心率/血糖/体重 | 数字 | - |
| 血糖类型 | 单选 | 空腹、餐后 |
| 精神状态 | 单选 | 良好、一般、较差、嗜睡、烦躁 |
| 记录人 | 关联 | → 员工表.姓名 |
| 异常标记 | 公式 | `IF(OR(体温<36,体温>37.3,收缩压>140,收缩压<90,舒张压>90,舒张压<60,血糖>7.8,血糖<3.9),"异常","正常")` |

#### 用药方案表

| 字段 | 改为 | 选项 |
|------|------|------|
| 老人姓名 | 关联 | → 老人基本信息表.姓名 |
| 服药频率 | 单选 | 每日1次、每日2次、每日3次、按需、其他 |
| 服药方式 | 单选 | 餐前、餐后、睡前、空腹、随餐 |
| 状态 | 单选 | 服用中、已停药、已完成 |

#### 服药记录表

| 字段 | 改为 | 选项 |
|------|------|------|
| 老人姓名 | 关联 | → 老人基本信息表.姓名 |
| 服药状态 | 单选 | 已服、拒绝、漏服、稍后 |
| 操作人 | 关联 | → 员工表.姓名 |

#### 药品库存表

| 字段 | 改为 | 公式 |
|------|------|------|
| 当前库存(盒) ~ 预警阈值(盒) | 数字 | - |
| 预计耗尽日期 | 公式 | `TODAY() + 当前库存(盒) * 单盒用量(片) / 日均消耗(片)` |
| 库存状态 | 公式 | `IF(当前库存(盒)<=预警阈值(盒),"预警",IF(当前库存(盒)<=预警阈值(盒)*1.5,"偏低","充足"))` |

#### 排班表

| 字段 | 改为 | 选项 |
|------|------|------|
| 护理员 | 关联 | → 员工表.姓名 |
| 班次 | 单选 | 白班(08-16)、中班(16-24)、夜班(00-08) |
| 负责区域 | 单选 | A区、B区、C区、全院 |
| 状态 | 单选 | 正常、请假、调班、空缺 |
| 替班人/排班人 | 关联 | → 员工表.姓名 |

#### 健康数据表

| 字段 | 改为 | 选项/公式 |
|------|------|-----------|
| 老人姓名 | 关联 | → 老人基本信息表.姓名 |
| 体温 ~ 体重 | 数字 | - |
| 血糖类型 | 单选 | 空腹、餐后 |
| 记录人 | 关联 | → 员工表.姓名 |
| 异常等级 | 公式 | `IF(OR(体温>38,体温<35.5,收缩压>160,收缩压<80,舒张压>100,舒张压<50,心率>120,心率<40,血糖>11,血糖<2.8),"危险",IF(OR(体温>37.3,体温<36,收缩压>140,收缩压<90,舒张压>90,舒张压<60,心率>100,心率<60,血糖>7.8,血糖<3.9),"偏高偏低","正常"))` |

#### 费用台账表

| 字段 | 改为 | 选项 |
|------|------|------|
| 老人姓名 | 关联 | → 老人基本信息表.姓名 |
| 费用类别 | 单选 | 床位费、护理费、餐费、医疗费、额外服务 |
| 计费周期 | 单选 | 月结、日结、次结 |
| 状态 | 单选 | 未结算、已结算、已开票 |

#### 收费标准表

| 字段 | 改为 | 选项 |
|------|------|------|
| 费用类别 | 单选 | 床位费、护理费、餐费、医疗费、额外服务 |
| 护理等级 | 单选 | 自理、半护理、全护理、特护 |

#### 活动方案表

| 字段 | 改为 | 选项 |
|------|------|------|
| 活动类型 | 单选 | 运动、音乐、手工、认知、社交、节日 |
| 适合能力等级 | 多选 | 自理、半护理、全护理、认知障碍 |
| 难度等级 | 单选 | 简单、中等、较难 |

#### 活动记录表

| 字段 | 改为 | 选项/公式 |
|------|------|-----------|
| 活动名称 | 关联 | → 活动方案表.活动名称 |
| 活动类型 | 单选 | 运动、音乐、手工、认知、社交、节日 |
| 参与度 | 单选 | 高、中、低 |
| 老人反馈 | 单选 | 积极、中性、消极 |
| 带领人 | 关联 | → 员工表.姓名 |
| 参与率 | 公式 | `IF(应参加人数>0,实参加人数/应参加人数,0)` |

#### 餐饮菜单表

| 字段 | 改为 | 选项 |
|------|------|------|
| 餐次 | 单选 | 早餐、午餐、晚餐、加餐 |
| 营养标签 | 多选 | 低盐、低糖、低脂、高蛋白、高纤维 |
| 适合饮食类型 | 多选 | 普食、软食、半流食、流食 |

#### 餐饮反馈表

| 字段 | 改为 | 选项 |
|------|------|------|
| 餐次 | 单选 | 早餐、午餐、晚餐、加餐 |
| 进食量 | 单选 | 全部吃完、大半、一半、少量、拒食 |
| 反馈 | 单选 | 喜欢、一般、不喜欢 |
| 记录人 | 关联 | → 员工表.姓名 |

#### 员工表

| 字段 | 改为 | 选项 |
|------|------|------|
| 岗位 | 单选 | 护理员、护士、厨师、保洁、行政、院长 |
| 职级 | 单选 | 初级、中级、高级、主管 |
| 状态 | 单选 | 在职、离职、休假 |

#### 家属联系表

| 字段 | 改为 | 选项 |
|------|------|------|
| 关系 | 单选 | 子女、配偶、其他 |
| 对应老人 | 关联 | → 老人基本信息表.姓名 |
| 推送频率 | 单选 | 每日、每周、不推送 |

### 第4步：清理示例数据

CSV 自带 2-3 行示例数据，设好字段类型后删掉，录入真实数据。**先录员工表和老人基本信息表**，其他表的关联字段才能正常工作。

### 第5步：配置自动化

在多维表格中点 `自动化` → `新建自动化`，逐条配置以下 6 条规则。

#### 自动化 1：异常体征预警

```
触发条件：护理记录表 → 异常标记 = "异常"
执行动作：发送飞书消息 → 接收人：主管
消息内容：【预警】{老人姓名}体征异常：体温{体温}，血压{收缩压}/{舒张压}，请及时查看。
```

操作步骤：
1. 点 `自动化` → `新建自动化`
2. 触发条件选 `记录被修改` → 数据表选 `护理记录表` → 字段选 `异常标记` → 条件选 `等于` → 值填 `异常`
3. 执行动作选 `发送消息` → 选接收人（主管的飞书账号）
4. 消息模板填上面的内容，字段用 `{}` 引用

#### 自动化 2：漏服预警

```
触发条件：服药记录表 → 服药状态 = "漏服"
执行动作：发送加急消息 → 接收人：主管+当班护理员
消息内容：【漏服预警】{老人姓名}应服{药品名称}（{应服时间}）未确认服药，请立即处理。
```

#### 自动化 3：药品库存预警

```
触发条件：药品库存表 → 库存状态 = "预警"
执行动作：发送消息 → 接收人：采购员
消息内容：【库存预警】{药品名称}当前库存{当前库存(盒)}盒，已低于预警阈值，请尽快采购。
```

#### 自动化 4：每日护理摘要

```
触发条件：定时触发 → 每天 20:00
执行动作：
  1. 查询护理记录表当天记录
  2. 按老人汇总
  3. 发送消息给推送频率为"每日"的家属
消息模板：尊敬的家属，{老人姓名}今日情况：体温{体温}，血压{收缩压}/{舒张压}，精神{精神状态}，饮食{饮食情况}。
```

操作步骤：
1. 触发条件选 `定时触发` → 设为每天 20:00
2. 执行动作选 `发送消息` → 接收人选 `家属联系表中推送频率=每日的家属`
3. 消息模板用字段引用

#### 自动化 5：每日用药清单

```
触发条件：定时触发 → 每天 07:00
执行动作：
  1. 查询用药方案表中状态=服用中的记录
  2. 按老人汇总当日用药清单
  3. 发送消息给当班护理员
消息模板：今日用药清单：\n{老人姓名}：{药品名称} {剂量}（{服药时间}）\n...
```

#### 自动化 6：月度账单

```
触发条件：定时触发 → 每月1日 08:00
执行动作：
  1. 查询费用台账表中状态=未结算的记录
  2. 按老人汇总
  3. 发送账单消息给家属
消息模板：{老人姓名}{账单月份}账单：\n床位费 {金额}元\n护理费 {金额}元\n合计 {总金额}元
```

> **提示**：自动化规则配置后，先手动触发测试一次，确认消息能正常发送。

---

## 路线 B：API 一键建表（会编程选这个）

```bash
cd scripts
npm install
cp .env.example .env
# 编辑 .env 填入三个值：
#   APP_TOKEN  - 多维表格 URL 里 /base/ 后面那段
#   APP_ID     - 飞书开放平台应用 ID
#   APP_SECRET - 飞书开放平台应用密钥
npm start      # 自动创建 15 张表 + 所有字段 + 关联 + 公式
npm run seed   # 写入演示数据（6名员工、5位老人、7天护理记录等）
npm run verify # 验证是否全部创建成功
```

看到演示数据后，在飞书多维表格中全选删除，再录真实数据即可。

如果 verify 显示公式字段缺失，按路线 A 第3步中的公式手动补上即可。

**注意：API 建表只创建表和字段，不包含自动化规则。** 表建完后请回到上面的第5步配置 6 条自动化规则，否则预警、推送、汇总等功能不会生效。

飞书开放平台注册地址：https://open.feishu.cn/app
需开通权限：`bitable:app`

---

## 第6步（两条路线都要做）：配置仪表盘

在每个数据表中点 `仪表盘`，添加以下图表：

| 数据表 | 图表 | 类型 |
|--------|------|------|
| 护理记录表 | 今日异常记录 | 柱状图 |
| 健康数据表 | 血压趋势(近30天) | 折线图 |
| 健康数据表 | 血糖趋势(近30天) | 折线图 |
| 排班表 | 今日在岗人员 | 分组表格 |
| 排班表 | 月度夜班统计 | 柱状图 |
| 费用台账表 | 月度收入构成 | 饼图 |
| 费用台账表 | 收入趋势(近12月) | 折线图 |
| 活动记录表 | 活动类型分布 | 饼图 |
| 餐饮反馈表 | 受欢迎菜品 TOP5 | 柱状图 |

---

## 第7步：安装 AI 技能

把 `SKILL.md` 的内容复制到你的 TRAE AI 助手中新建一个 Skill，之后可以直接对 AI 说：

- "记录张奶奶今天血压 135/85，吃了降压药"
- "生成本周排班表"
- "发送张奶奶的家属周报"
- "查一下李德福这周的血糖趋势"

---

## 检查清单

全部打勾就可以投入使用：

- [ ] 15 张表已创建
- [ ] 字段类型已设置（单选/多选/数字/关联/公式）
- [ ] 公式字段已添加（异常标记、异常等级、库存状态、参与率、年龄）
- [ ] 示例数据已清理
- [ ] 真实数据已录入（先录员工表和老人基本信息表）
- [ ] 异常预警自动化已配置
- [ ] 漏服预警自动化已配置
- [ ] 库存预警自动化已配置
- [ ] 每日摘要推送已配置
- [ ] 用药清单推送已配置
- [ ] 月度账单已配置
- [ ] 仪表盘已配置
- [ ] SKILL 已安装到 AI 助手
- [ ] 测试录入一条护理记录
- [ ] 测试一次家属推送

---

## 常见问题

**CSV 导入后字段全是文本类型？**
正常。CSV 不携带类型信息，按第3步手动设置。

**关联字段怎么设？**
新增字段 → 选"关联" → 选要关联的表 → 选关联字段。必须先有被关联的表才能设关联，所以先录员工表和老人基本信息表。

**公式字段报错？**
飞书公式语法与 Excel 类似但略有不同。如果报错，检查字段名是否完全一致（含括号等特殊字符），或参考飞书官方公式文档。

**没有飞书企业版？**
免费版飞书也能用多维表格，但自动化触发次数有限。建议至少用标准版。

**不想用 TRAE AI？**
可以。工作流文档是通用方案，任何能读飞书多维表格的 AI 工具都能配合使用。
