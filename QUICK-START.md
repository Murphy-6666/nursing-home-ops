# 60 分钟快速上手

> 不会编程也行，跟着做就行。会编程的直接跳到最后。

---

## 你将得到什么

- 15 张相互关联的飞书多维表格，覆盖养老院全部日常运营
- 6 条自动化规则（异常预警、漏服提醒、库存预警、每日摘要等）
- 9 个数据仪表盘图表
- AI 技能，语音/文字直接录入数据

---

## 第 1 步：下载项目（2 分钟）

到 https://github.com/Murphy-6666/nursing-home-ops 点 `Code` → `Download ZIP`，解压。

## 第 2 步：导入 CSV（10 分钟）

1. 打开飞书，新建一个多维表格
2. 点右上角 `...` → `导入` → 选 CSV
3. 把 `templates/csv/` 下的 15 个 CSV 文件依次导入

导入后你会得到 15 个数据表，每个带 2-3 行示例数据。

## 第 3 步：设置字段类型（25 分钟）

CSV 导入后所有字段都是"文本"，需要改成正确类型。**完整字段类型对照表见 [SETUP.md](SETUP.md) 第 3 步。**

优先级：
1. **先设基础表**：老人基本信息表、员工表（设好这两张，其他表的关联才能用）
2. **再设业务表**：护理记录表、用药方案表、排班表等

关键操作：
- 单选/多选：点字段名 → 改类型 → 填选项
- 数字：点字段名 → 改为"数字"
- 关联：点字段名 → 改为"关联" → 选目标表 → 选关联字段
- 公式：点字段名 → 改为"公式" → 粘贴公式

**最核心的两张表先设好：**

### 老人基本信息表

| 字段 | 改为 | 选项/公式 |
|------|------|-----------|
| 性别 | 单选 | 男、女 |
| 年龄 | 公式 | `DATEDIF(出生年月, TODAY(), "Y")` |
| 护理等级 | 单选 | 自理、半护理、全护理、特护 |
| 饮食类型 | 单选 | 普食、软食、半流食、流食 |
| 饮食禁忌 | 多选 | 糖尿病饮食、低盐、低脂、低嘌呤、无麸质 |
| 状态 | 单选 | 在住、出院、请假 |

### 护理记录表

| 字段 | 改为 | 选项/公式 |
|------|------|-----------|
| 老人姓名 | 关联 | → 老人基本信息表.姓名 |
| 班次 | 单选 | 白班、中班、夜班 |
| 体温/收缩压/舒张压/心率/血糖 | 数字 | - |
| 异常标记 | 公式 | `IF(OR(体温<36,体温>37.3,收缩压>140,收缩压<90,舒张压>90,舒张压<60,血糖>7.8,血糖<3.9),"异常","正常")` |

其余 13 张表的字段类型对照，见 [SETUP.md](SETUP.md) 第 3 步。

## 第 4 步：清理示例数据（1 分钟）

全选每张表的行 → 删除。然后录入真实数据：**先录员工表和老人基本信息表**。

## 第 5 步：配置自动化（15 分钟）

在多维表格中点 `自动化` → `新建自动化`，配置以下 6 条规则。每条规则的详细配置见 [SETUP.md](SETUP.md) 第 5 步。

| 序号 | 规则名称 | 触发条件 | 执行动作 |
|------|---------|---------|----------|
| 1 | 异常体征预警 | 护理记录表.异常标记 = "异常" | 推送消息给当班主管 |
| 2 | 漏服药品预警 | 服药记录表.服药状态 = "漏服" | 加急通知护士长 |
| 3 | 药品库存预警 | 药品库存表.库存状态 = "预警" | 推送采购提醒给行政 |
| 4 | 每日护理摘要 | 每天 20:00 定时触发 | 汇总当日记录，推送家属 |
| 5 | 每日用药清单 | 每天 08:00 定时触发 | 推送当日用药清单给护理员 |
| 6 | 月度账单生成 | 每月 1 日定时触发 | 汇总上月费用，生成账单 |

## 第 6 步：验证搭建结果（2 分钟）

做完上面 5 步后，用以下 3 个测试确认系统正常：

1. **录一条护理记录**：在护理记录表手动添加一行，体温填 38.5，看「异常标记」是否自动变成"异常"
2. **测试自动化**：手动触发规则1，看主管是否收到飞书消息
3. **检查仪表盘**：点 `仪表盘` → 添加图表，看示例数据能否正常展示

更完整的检查清单（15 项）见 [SETUP.md](SETUP.md) 底部的检查清单。

---

## 常见问题

**Q：CSV 导入后字段全是文本类型怎么办？**
A：正常，按第 3 步逐个改成正确类型就行。

**Q：关联字段灰色选不了？**
A：先录员工表和老人基本信息表的数据，关联字段需要目标表有记录才能选。

**Q：公式字段报错？**
A：检查公式里引用的字段名是否和实际字段名完全一致，包括括号。比如"当前库存(盒)"的括号是英文括号还是中文括号。

**更多问题** 看 [SETUP.md](SETUP.md) 底部的常见问题。

---

## 建表完成后

1. 打开飞书多维表格，查看 15 张表是否都在
2. 点 `仪表盘` 添加图表（参考 [SETUP.md](SETUP.md) 第 6 步）
3. 把 `SKILL.md` 复制到 `.trae/skills/nursing-home-ops/SKILL.md`，就可以用语音录入了
4. 对 AI 说「查一下今天谁值白班」验证技能是否生效

---

## 会编程？一键搞定

跳过上面第 2-5 步，用脚本自动完成：

```bash
git clone https://github.com/Murphy-6666/nursing-home-ops.git
cd nursing-home-ops/scripts
npm install
cp .env.example .env
# 编辑 .env 填入 APP_TOKEN、APP_ID、APP_SECRET
npm start          # 自动建 15 张表 + 所有字段 + 关联 + 公式
npm run seed       # 写入演示数据（6 名员工、5 位老人、7 天护理记录等）
npm run verify     # 验证是否全部正确
```

看到演示数据后，全选删除再录真实数据即可。自动化规则仍需手动配置（飞书 API 暂不支持自动创建自动化），参考第 5 步。
